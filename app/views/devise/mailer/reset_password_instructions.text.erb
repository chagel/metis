Reset your password

We received a request to reset the password for your Metis account (<%= @resource.email %>).

Reset it here:
<%= edit_password_url(@resource, reset_password_token: @token) %>

If you didn't request this, you can safely ignore this email — your password won't change until you open the link above and choose a new one.

—
Metis · Your AI agent workspace
