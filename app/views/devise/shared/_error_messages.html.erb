<% if resource.errors.any? %>
  <div class="auth-error">
    <strong><%= t('.intro', count: resource.errors.count) %></strong>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
