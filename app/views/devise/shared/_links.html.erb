<div class="auth-links">
  <% if controller_name != "sessions" %>
    <%= link_to "Sign in", new_session_path(resource_name), class: "auth-link" %>
  <% end %>

  <% if devise_mapping.registerable? && controller_name != "registrations" && registration_offered? %>
    <%= link_to "Create an account", new_registration_path(resource_name), class: "auth-link" %>
  <% end %>

  <% if devise_mapping.recoverable? && controller_name != "passwords" && controller_name != "registrations" %>
    <%= link_to "Forgot your password?", new_password_path(resource_name), class: "auth-link" %>
  <% end %>
</div>
