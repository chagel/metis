<%# locals: (active:) — unread indicator on the sidebar "Shared" tab.
    Broadcast on (active: true) when a teammate shares a conversation;
    re-rendered hidden when the tab bar reloads (any tab click). %>
<span id="shared-tab-dot" class="tab-dot"<%= " hidden" unless active %>></span>
