<%# locals: (active:) — unread indicator on the sidebar "Team" tab.
    Broadcast on (active: true) when a teammate opens a conversation to
    the team; re-rendered hidden when the tab bar reloads (any tab click). %>
<span id="team-tab-dot" class="tab-dot"<%= " hidden" unless active %>></span>
