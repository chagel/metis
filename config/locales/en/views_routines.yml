en:
  routines:
    index:
      title: "Routines"
      new: "New"
      empty: "No routines yet. A routine is a saved prompt that runs on its own — on a schedule or when a webhook event arrives — as a normal agent turn."
      create_first: "Create your first routine"
    routine:
      disabled: "Paused"
      schedule: "%{cron} · %{tz}"
      event: "On %{type}"
      project_suffix: "· %{name}"
      run: "Run now"
      pause: "Pause"
      resume: "Resume"
    new:
      title: "New routine"
    edit:
      back_to_routines: "Back to routines"
      delete_confirm: "Delete %{name}?"
    form:
      name_placeholder: "Morning standup digest"
      prompt_placeholder: "Summarize what changed across the team's projects since yesterday and post the highlights."
      prompt_hint_html: "Plain instructions for the agent — it runs as a normal turn, so it can use connectors, skills, or start a workflow. Use <code>{{date}}</code>, <code>{{team}}</code>, <code>{{user}}</code>, and on event triggers <code>{{event_type}}</code> / <code>{{event_payload}}</code>."
      trigger_label: "Trigger"
      trigger_kinds:
        schedule: "On a schedule"
        webhook: "On a webhook event"
      schedule_label: "Schedule"
      frequency_label: "Frequency"
      frequencies:
        hourly: "Hourly"
        daily: "Daily"
        weekly: "Weekly"
        monthly: "Monthly"
        custom: "Custom (cron)"
      hour_label: "Hour"
      minute_label: "Minute"
      days_label: "Days"
      day_abbr:
        mon: "Mon"
        tue: "Tue"
        wed: "Wed"
        thu: "Thu"
        fri: "Fri"
        sat: "Sat"
        sun: "Sun"
      day_of_month_label: "Day of month"
      cron_label: "Cron expression"
      cron_hint_html: "A 5-field cron expression, evaluated in the time zone below."
      preview:
        daily: "Every day at {time} · {tz}"
        weekly: "{days} at {time} · {tz}"
        hourly: "Every hour at {minute} · {tz}"
        monthly: "Day {dom} of every month at {time} · {tz}"
        custom: "cron {cron} · {tz}"
        every_day: "Every day"
      timezone_label: "Time zone"
      timezone_hint: "The schedule runs in this time zone."
      event_label: "Event type"
      event_blank: "Select an event…"
      event_current: "Current"
      event_hint_html: "The webhook event that fires this routine. You can only pick an event type your team has actually received (e.g. <code>pull_request.opened</code>, <code>Issue.create</code>), plus a <code>.*</code> wildcard per family (<code>pull_request.*</code>). The list is empty until your team's GitHub/Linear webhooks are configured and events arrive."
      cooldown_label: "Cooldown (seconds)"
      cooldown_hint: "Minimum gap between fires for bursty events. 0 = no cooldown."
      visibility_label: "Visibility"
      visibilities:
        personal: "Personal — only you see the runs"
        team: "Team — any member can open the runs"
      visibility_hint: "Each fire starts a conversation; this sets who can see it."
      project_label: "Project"
      project_blank: "None"
      project_hint: "Optional — gives the run a project's repo and standards as context."
      model_label: "Model"
      model_default: "Deployment default"
      model_hint: "The model each fire runs on. Defaults to the deployment's configured model."
      create_routine: "Create routine"
